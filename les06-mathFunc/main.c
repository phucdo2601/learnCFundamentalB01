#include <stdio.h>
#include <math.h>

int main()
{
    double A = sqrt(9);
    double B = pow(2, 4);
    double C = round(3.14);
    double D = ceil(3.14);
    int E = floor(3.99);
    double F = fabs(-100);
    double G = log(3);
    double H = sin(45);
    double I = tan(45);
    double J = cos(45);

    printf("A = %lf\n", A);

    return 0;
}