#include <stdio.h>

int main() {
    // logical operatos = && (And) checks if two conditions are true
    float temp = 25;

    if (temp > 10 && temp < 50)
    {
        printf("\nThe tempertuarer is avarage!");
    } else {
        printf("\nThe temperature is unavaiable!");
    }
    
}